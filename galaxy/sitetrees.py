# from sitetree.utils import tree, item
#
# # Be sure you defined `sitetrees` in your module.
# sitetrees = (
#   # Define a tree with `tree` function.
#   tree('galaxy_all', items=[
#       # Then define items and their children with `item` function.
#       item('GALAXY', 'galaxy.addgi', children=[
#           item('Register Galaxy instance', 'addgi', in_menu=True, in_sitetree=True),
#           item('Register Galaxy user', 'addguser', in_menu=True, in_sitetree=True),
#           item('Add Workflow', 'addworkflow', in_menu=True, in_sitetree=True),
#           item('View & Run Workflows', 'workflow_summary', in_menu=True, in_sitetree=True),
#           item('Add data files to Galaxy datalibrary', 'galaxy_files_to_datalib', in_menu=True, in_sitetree=True),
#           item('Add data files to Galaxy history', 'galaxy_files_to_history', in_menu=True, in_sitetree=True),
#           item('Workflow status', 'workflow_status', in_menu=True, in_sitetree=True),
#           item('History status', 'history_status', in_menu=True, in_sitetree=True),
#       ])
#   ]),
#   # ... You can define more than one tree for your app.
# )
